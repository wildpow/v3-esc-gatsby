import React from "react"

const MattressList = ({
  mattresses,
  title,
  description,
  breadCrumbs,
  brandName,
  headerBG,
  button,
}) => {
  return <div breadCrumbs={breadCrumbs}>srdgre</div>
}

export default MattressList
